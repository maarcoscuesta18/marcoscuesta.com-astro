---
import { Icon } from "astro-icon";
const { timeframe, title, description, url, tags, url_name } = Astro.props
---

<div class="grid grid-cols-3 grid-rows-1 w-full p-2 place-items-center border-b-[1px] border-dashed py-8 border-gray-700">
    <div class="self-start justify-self-start">
        <p class=" text-base">
            { timeframe }
        </p>
    </div>

    <div class="self-start justify-self-start text-left">
        <p class="text-lg ">{title}</p>
        <p class="text-sm ">{description}</p>
        <sub class="text-xs ">{tags}</sub>
    </div>

    <div class="self-start justify-self-end">
        <a href={url} target="_blank" class="text-base text-right  flex justify-center items-center">
            {url_name}
            {
               url !== "#" && <Icon name="icon-park-outline:unlink" class="w-4 h-4 ml-2" />
            }
        </a>
    </div>
</div>